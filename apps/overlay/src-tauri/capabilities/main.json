{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows the main overlay window to use core Tauri APIs and listen for backend overlay-state events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
